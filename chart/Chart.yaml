apiVersion: v2
name: k8s-spark-ui-assist
description: >
  Discovers running Spark driver pods in a namespace and serves a web page
  linking to each job's Spark UI. Optionally manages Gateway API HTTPRoutes for
  each driver pod automatically.
type: application
version: 1.1.3
appVersion: 1.1.3
keywords:
  - spark
  - kubernetes
  - gateway-api
sources:
  - https://github.com/unaiur/k8s-spark-ui-assist
